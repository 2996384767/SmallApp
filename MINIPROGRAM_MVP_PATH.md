# 肃联跨境小程序 MVP 实现路径

## 路线结论

当前项目先按“比赛可演示的真实 MVP”推进，不做完整跨境电商平台，也不优先做 H5 展示页、微服务、Redis、消息队列、ERP、支付、物流 API 或 AI 匹配。

当前执行技术路线：

```text
微信原生小程序
    |
    | HTTPS / JSON
    v
Flask API + Flask/Jinja2 管理后台
    |
    v
本地 MariaDB 开发库
```

现有 `uni-app / Vue 3` 代码可以作为早期学习和页面参考，但比赛 MVP 的主线暂时切换为微信原生小程序 + Flask + MariaDB。

## 产品边界

比赛版本产品定义：

> 肃联跨境：肃宁针纺产业带海外需求撮合与订单进度管理小程序 MVP

核心链路固定为：

```text
需求录入
  -> 平台审核脱敏
  -> 需求池发布
  -> 工厂查看和筛选
  -> 申请接单
  -> 平台审核
  -> 形成订单
  -> 进度同步
```

本版本明确不做：

- 在线支付
- 报关系统
- 物流 API
- 海外买家端
- AI 自动匹配
- ERP 对接
- 自动对接第三方跨境平台

## 小程序端

底部 Tab 只保留三个：

```text
需求大厅
我的接单
我的
```

第一版登录使用企业账号和密码，不做复杂微信授权。登录成功后后端返回 Token，小程序保存到本地缓存。

比赛演示账号提前准备：

```text
factory001 / 123456
factory002 / 123456
factory003 / 123456
```

核心页面：

1. 登录页：企业账号、密码、登录状态保存。
2. 需求大厅：展示已发布需求，支持国家、品类、工艺、交期筛选。
3. 需求详情：展示脱敏后的需求信息、服装图片、数量、工艺、交期等字段。
4. 申请接单：工厂提交接单申请，状态进入待审核。
5. 我的接单：展示本厂申请记录，区分待审核、已通过、已完成。
6. 订单详情 / 进度：展示订单编号、产品信息和当前进度节点。
7. 我的：展示工厂信息、退出登录等基础功能。

需求详情必须展示：

| 字段 | 规则 |
| --- | --- |
| 需求编号 | 必须 |
| 国家 | 必须 |
| 产品品类 | 必须 |
| 产品图片 | 必须 |
| 数量 | 必须 |
| 工艺 | 必须 |
| 交期 | 必须 |
| 面料 | 推荐 |
| 颜色 | 推荐 |
| 尺码 | 推荐 |
| 特殊要求 | 推荐 |
| 客户姓名 | 不展示 |
| Email | 不展示 |
| WhatsApp | 不展示 |
| 客户公司名 | 原则上脱敏 |

## 管理后台

后台不单独开发 React/Vue。为了比赛速度，直接使用：

```text
Flask + Jinja2 + Bootstrap
```

后台核心模块：

- 需求管理：新增、修改、审核、发布、下架需求。
- 接单管理：查看工厂申请，批准或拒绝。
- 订单管理：查看订单，更新订单状态和进度。
- 工厂管理：维护工厂账号、主营品类、工艺能力和启用状态。

真实业务录入流程：

```text
海外需求表
  -> 工作人员翻译
  -> 工作人员脱敏
  -> 后台录入
  -> 发布到小程序需求池
```

## 数据库表

比赛 MVP 先用 5 张业务表 + 1 张管理员表：

- `factory`：工厂账号和能力标签。
- `demand`：脱敏后的海外采购需求。
- `demand_application`：工厂接单申请，必须设置 `factory_id + demand_id` 唯一约束。
- `orders`：平台审核通过后形成的正式订单。
- `order_progress`：订单进度节点。
- `admin`：后台管理员账号。

本地开发数据库：

```text
MariaDB
Host: 127.0.0.1
Port: 3306
Database: smallapp
```

## API 范围

第一版 API 控制在 15 个左右：

```text
认证
POST   /api/login

需求
GET    /api/demands
GET    /api/demands/<id>

接单
POST   /api/demands/<id>/apply
GET    /api/my/applications

订单
GET    /api/my/orders
GET    /api/orders/<id>
GET    /api/orders/<id>/progress

后台
POST   /api/admin/login

GET    /api/admin/demands
POST   /api/admin/demands
PUT    /api/admin/demands/<id>
DELETE /api/admin/demands/<id>

GET    /api/admin/applications
POST   /api/admin/applications/<id>/approve
POST   /api/admin/applications/<id>/reject

GET    /api/admin/orders
PUT    /api/admin/orders/<id>/status
```

## 图片处理

比赛阶段使用授权服装图片，不抓取或盗用第三方图片。

第一版处理方式：

```text
管理员上传图片
  -> Flask 保存到 uploads/
  -> 数据库保存图片 URL
  -> 小程序展示图片
```

后期再迁移到腾讯云 COS 或其他对象存储。

## 开发顺序

不要先逐页美化。按业务闭环推进：

1. 数据库建表。
2. Flask API 骨架。
3. 登录、需求列表、需求详情。
4. 申请接单、我的接单、防重复申请。
5. 后台管理、审核申请、生成订单。
6. 订单详情、订单进度、后台更新进度。
7. UI 美化、服装图片、测试数据、异常处理。
8. 服务器部署、HTTPS、真机测试。

## 时间安排

以 9 月 3 日为起点：

| 时间 | 目标 |
| --- | --- |
| 9 月 3-5 日 | 数据库、Flask 项目、用户登录、需求 CRUD |
| 9 月 6-8 日 | 需求大厅、筛选、详情、图片展示 |
| 9 月 9-10 日 | 申请接单、我的接单、防重复申请 |
| 9 月 11-12 日 | 后台、申请审核、生成订单 |
| 9 月 13-14 日 | 订单进度、后台修改进度 |
| 9 月 15-17 日 | UI、服装图片、测试数据、异常处理 |
| 9 月 18 日前 | 真机完整演示业务流程 |

## 演示脚本

比赛现场建议使用电脑 + 手机两台设备：

1. 电脑打开管理后台，新增并发布一个泰国 T 恤需求。
2. 手机打开小程序，刷新需求大厅，看到新需求。
3. 手机进入需求详情，查看图片和脱敏需求信息。
4. 手机点击申请接单。
5. 电脑后台看到工厂申请，点击确认承接。
6. 手机进入我的接单，看到状态已通过。
7. 手机进入订单详情，查看订单进度。
8. 电脑后台更新进度为生产中或集货质检。
9. 手机刷新后看到进度同步变化。

目标是在 2-3 分钟内跑完整个闭环。
